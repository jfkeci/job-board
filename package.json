{
  "name": "job-board-clone",
  "private": true,
  "pnpm": {
    "overrides": {
      "@types/react": "^19.0.2",
      "@types/react-dom": "^19.0.2"
    }
  },
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "dev:api": "turbo dev --filter=@job-board/api",
    "dev:web": "turbo dev --filter=@job-board/web",
    "dev:dashboard": "turbo dev --filter=@job-board/dashboard",
    "dev:admin": "turbo dev --filter=@job-board/admin",
    "cli": "pnpm --filter=@job-board/api cli",
    "db:status": "pnpm --filter=@job-board/api cli db:status",
    "db:seed": "pnpm --filter=@job-board/api cli db:seed",
    "db:purge": "pnpm --filter=@job-board/api cli db:purge",
    "db:reset": "pnpm --filter=@job-board/api cli db:reset",
    "lint": "turbo lint",
    "format": "turbo format",
    "format:check": "turbo format:check",
    "type-check": "turbo type-check",
    "test": "turbo test",
    "clean": "turbo clean && rm -rf node_modules",
    "clean:api": "pnpm --filter=@job-board/api clean",
    "clean:web": "pnpm --filter=@job-board/web clean",
    "clean:dashboard": "pnpm --filter=@job-board/dashboard clean",
    "clean:admin": "pnpm --filter=@job-board/admin clean",
    "reinstall": "turbo clean && rm -rf node_modules && pnpm install"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.7.2"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  }
}
